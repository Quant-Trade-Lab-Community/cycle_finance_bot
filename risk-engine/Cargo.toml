[package]
name = "risk-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
parking_lot = { workspace = true }
rust_decimal = { workspace = true }
strategies-engine = { path = "../strategies-engine" }
