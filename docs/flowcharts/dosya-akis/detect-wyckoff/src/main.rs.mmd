graph TD
    A([Başlangıç: main&#40;&#41;]) --> B[Banner yazdır]
    B --> C[state = Arc&#40;AppState&#123; client: BinanceClient::new&#125;&#41;]
    C --> D[Router: /api/wyckoff -> get_wyckoff]
    D --> E[addr = 127.0.0.1:3005]
    E --> F[TcpListener bind]
    F --> G[axum::serve: sonsuz istek döngüsü]

    subgraph get_wyckoff
        H[get_wyckoff&#40;Query params, State&#41;] --> I[limit = params.limit veya 500]
        I --> J[state.client.fetch_klines&#40;symbol, interval, limit&#41;]
        J --> K{başarılı mı?}
        K -- Hayır --> L[Json hata: error + message]
        K -- Evet --> M{klines boş mu?}
        M -- Evet --> N[Json: "No data received"]
        M -- Hayır --> O[current_price = son kline kapanışı]
        O --> P[cfg = AnalysisConfig::default&#40;&#41;]
        P --> Q[analyst::analyze&#40;&klines, &cfg&#41;]
        Q --> R{analiz başarılı mı?}
        R -- Evet --> S[Json: status success + insight]
        R -- Hayır --> T[Json: error + mesaj]
    end
