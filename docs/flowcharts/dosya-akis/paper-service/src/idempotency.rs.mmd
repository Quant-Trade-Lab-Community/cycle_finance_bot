graph TD
    A([Başlangıç]) --> B[InMemoryIdempotencyCache: Mutex HashMap yapısı]

    B --> G[get(client_oid) çağrısı]
    G --> GC{inner HashMap'te kayıt var mı?}
    GC -- Evet --> GD[cloned CachedResponse dön]
    GC -- Hayır --> GN[None dön]

    B --> S[set(client_oid, response) çağrısı]
    S --> SI[inner.lock + HashMap insert]
    SI --> Z([Bitiş])
