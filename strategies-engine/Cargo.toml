[package]
name = "strategies-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
transport = { path = "../cycle-engine/transport" }
rust_decimal = { workspace = true }
libc = { workspace = true }
